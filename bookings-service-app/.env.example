# Copy is optional — normally you run: npx wix env pull
# That writes .env.local with the real values (keep .env.local out of git).
#
# WIX_CLIENT_ID=
# WIX_CLIENT_INSTANCE_ID=
# WIX_CLIENT_PUBLIC_KEY=
# WIX_CLIENT_SECRET=
